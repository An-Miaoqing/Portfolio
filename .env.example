# Required for the /contact page form (src/app/api/contact/route.ts) to send email via Resend.
# 1. Create a free account at https://resend.com
# 2. Create an API key
# 3. Copy this file to .env.local and paste the key below (never commit .env.local)
RESEND_API_KEY=
